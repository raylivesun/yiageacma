#!/usr/bin/R

s7 <- c("apple", "banana", "pear", "pineapple")
s8 <- function(fruit, str) {
      if (fruit == fruit) {
          print(fruit);
      } else {
        return(fruit) 
      }
      if (str == "") {
          print(str);
      } else {
        return(str)
      }
}
